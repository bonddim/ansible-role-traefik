pull_request_rules:
  - name: Automatic merge my PR
    conditions:
      - author=bonddim
      - check-success=molecule
    actions:
      merge:
        method: squash
        strict: smart
        strict_method: rebase

  - name: Delete head branch after merge
    conditions:
      - merged
    actions:
      delete_head_branch: {}
